name: VaultMind Daily Video

on:
  schedule:
    - cron: '0 16 * * 2,4'    # Tue+Thu 17:00 CET
    - cron: '0 18 * * 1,3,5'  # Mon/Wed/Fri 19:00 CET
    - cron: '0 8 * * 6,0'     # Sat+Sun 09:00 CET
  workflow_dispatch:
    inputs:
      niche:
        description: 'Niche (reddit/dating/rich/lifehack/fact/scary/motivation/conspiracy/learn)'
        required: false
        default: ''

jobs:
  generate-video:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install system dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq ffmpeg espeak

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      - name: Download background music (if missing)
        run: |
          if [ ! -f "bg_music.mp3" ]; then
            curl -sL -o bg_music.mp3 \
              "https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3" \
              && echo "Music downloaded" || echo "Music unavailable, continuing"
          fi

      - name: Run pipeline
        env:
          GAMEPLAY_PATH:            ${{ github.workspace }}/gameplay_bg.mp4
          MUSIC_PATH:               ${{ github.workspace }}/bg_music.mp3
          GROQ_API_KEY:             ${{ secrets.GROQ_API_KEY }}
          ELEVENLABS_KEY:           ${{ secrets.ELEVENLABS_KEY }}
          ELEVEN_VOICE_ID:          ${{ secrets.ELEVEN_VOICE_ID }}
          PEXELS_KEY:               ${{ secrets.PEXELS_KEY }}
          YOUTUBE_TOKEN_B64:        ${{ secrets.YOUTUBE_TOKEN_B64 }}
          BUFFER_ACCESS_TOKEN:      ${{ secrets.BUFFER_ACCESS_TOKEN }}
          BUFFER_TIKTOK_PROFILE_ID: ${{ secrets.BUFFER_TIKTOK_PROFILE_ID }}
        run: |
          NICHE="${{ github.event.inputs.niche }}"
          if [ -n "$NICHE" ]; then
            python pipeline.py 1 --niche "$NICHE"
          else
            python pipeline.py 1
          fi

      - name: Commit dashboard.json
        if: always()
        run: |
          git config user.name  "VaultMind Bot"
          git config user.email "bot@vaultmind.auto"
          git add dashboard.json || true
          git diff --cached --quiet && echo "No changes" || \
            (git commit -m "📊 Dashboard [$(date '+%Y-%m-%d %H:%M')]" && git push)
